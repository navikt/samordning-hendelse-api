management:
  endpoint.prometheus.enabled: true
  endpoints.web.exposure.include:
    - prometheus
    - health

spring:
  security.oauth2.resourceserver.jwt.jwk-set-uri: ${JWK_SET_URI}
  datasource.hikari.maximum-pool-size: ${MAX_POOL_SIZE}
  jackson.serialization.WRITE_DATES_AS_TIMESTAMPS: false

DESIRED_YTELSESTYPER: "'ALDER', 'AFP'"
maskinporten.validation.permitAll:
  - 889640782 #NAV
